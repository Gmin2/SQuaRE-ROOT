# QEM (quantum error mitigation)

YAML profiles for error-mitigation strategies (probabilistic error cancellation, extrapolation, etc.) that may **reduce logical effort** or **effective noise** in resource models without replacing QEC.

Each file must follow `../Schemas.yaml` (`document_header` + `parameter_entry` per assumption).

## Profiles

| File | Role |
|------|------|
| `identity_no_overhead.yaml` | Placeholder: suppression factor 1.0 and shot/readout overhead multipliers of 1.0 |
